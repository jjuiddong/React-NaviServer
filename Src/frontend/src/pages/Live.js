import React from "react";
import LiveMapContainer from "../containers/LiveMapContainer";
import Header from "../components/Header";

const Live = () => {
  return (
    <div>
      <Header></Header>
      <LiveMapContainer></LiveMapContainer>
    </div>
  );
};

export default Live;
